spring:
  datasource:
    url: jdbc:h2:mem:accounts-test;MODE=MySQL
    username: sa
    password:
    driver-class-name: org.h2.Driver

  data:
    redis:
      host: 127.0.0.1
      port: 6380
      timeout: 3000